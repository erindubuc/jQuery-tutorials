
// when the dom is ready to bind events to, then fire or bind the events
$(document).on("ready", function() {
   
    
});

// when document is ready, do whatever you need to do...
$(function(){
    
    // what you want to happen when document is ready
   
    
});

// waits for the whole web page to load - if you have an image, it needs to load and THEN the event will fire
    //e.g. may want to calculate the height of an image if it's sliding in
$(window).on("load", function() {
    
    
});

$(window).load(function() {
    
});